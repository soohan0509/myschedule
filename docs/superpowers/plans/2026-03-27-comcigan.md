# 컴시간 알리미 연동 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** NEIS API 기반 시간표 조회를 컴시간 알리미(comcigan-parser)로 대체하고, 과목과 선생님을 `파이썬(김진)` 형식으로 표시한다.

**Architecture:** 브라우저가 Vercel 서버리스 함수 `/api/comcigan`을 호출하면, 함수가 comcigan-parser로 경기북과학고(코드: 12045) 시간표를 파싱해 JSON으로 반환한다. `timetable.js`의 `fetchTimetable()`이 이 엔드포인트를 호출하고, `renderTimetable()`이 `파이썬(김진)` 형식으로 렌더링한다.

**Tech Stack:** comcigan-parser (npm), Vercel Serverless Functions (Node.js), Vanilla JS

---

### Task 1: package.json 생성

**Files:**
- Create: `package.json`

- [ ] **Step 1: package.json 작성**

`/home/soohan/myschedule/package.json` 파일을 생성한다:

```json
{
  "dependencies": {
    "comcigan-parser": "^0.6.4"
  }
}
```

- [ ] **Step 2: 의존성 설치**

```bash
npm install
```

Expected: `node_modules/comcigan-parser` 디렉토리 생성됨.

- [ ] **Step 3: .gitignore에 node_modules 추가 확인**

```bash
cat .gitignore | grep node_modules || echo "node_modules" >> .gitignore
```

- [ ] **Step 4: 커밋**

```bash
git add package.json package-lock.json .gitignore
git commit -m "chore: comcigan-parser 의존성 추가"
```

---

### Task 2: Vercel 서버리스 함수 생성

**Files:**
- Create: `api/comcigan.js`

- [ ] **Step 1: api 디렉토리 생성 및 함수 작성**

`/home/soohan/myschedule/api/comcigan.js` 파일을 생성한다:

```js
const Timetable = require('comcigan-parser');

const SCHOOL_CODE = 12045;
let cache = null;
let cacheAt = null;
const CACHE_TTL = 6 * 60 * 60 * 1000; // 6시간

module.exports = async function handler(req, res) {
  const classNum = parseInt(req.query.class);
  const day = parseInt(req.query.day); // 0=월, 1=화, 2=수, 3=목, 4=금

  if (!classNum || classNum < 1 || classNum > 5 || isNaN(day) || day < 0 || day > 4) {
    return res.status(400).json({ error: 'invalid params' });
  }

  try {
    if (!cache || Date.now() - cacheAt > CACHE_TTL) {
      const t = new Timetable();
      await t.init();
      t.setSchool(SCHOOL_CODE);
      cache = await t.getTimetable();
      cacheAt = Date.now();
    }

    const dayData = (cache[1]?.[classNum]?.[day]) || [];
    const result = {};
    dayData.forEach(item => {
      if (item && item.subject) {
        result[item.classTime + '교시'] = { subject: item.subject, teacher: item.teacher };
      }
    });

    res.setHeader('Cache-Control', 's-maxage=21600');
    res.json(result);
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
};
```

- [ ] **Step 2: 로컬에서 함수 직접 테스트**

```bash
node -e "
process.argv = ['node', 'test', '--class=1', '--day=1'];
const handler = require('./api/comcigan.js');
const res = {
  _headers: {},
  statusCode: 200,
  setHeader(k, v) { this._headers[k] = v; },
  status(c) { this.statusCode = c; return this; },
  json(d) { console.log('STATUS:', this.statusCode); console.log(JSON.stringify(d, null, 2)); }
};
handler({ query: { class: '1', day: '1' } }, res);
"
```

Expected: 1학년 1반 화요일 시간표 JSON 출력. 예:
```json
{
  "1교시": { "subject": "생명1", "teacher": "임수" },
  ...
}
```

- [ ] **Step 3: 잘못된 파라미터 테스트**

```bash
node -e "
const handler = require('./api/comcigan.js');
const res = {
  statusCode: 200,
  setHeader() {},
  status(c) { this.statusCode = c; return this; },
  json(d) { console.log('STATUS:', this.statusCode, JSON.stringify(d)); }
};
handler({ query: { class: '9', day: '1' } }, res);
"
```

Expected: `STATUS: 400 {"error":"invalid params"}`

- [ ] **Step 4: 커밋**

```bash
git add api/comcigan.js
git commit -m "feat: 컴시간 서버리스 함수 추가 (경기북과학고 1학년)"
```

---

### Task 3: timetable.js — fetchTimetable 교체

**Files:**
- Modify: `timetable.js:1` (import 제거)
- Modify: `timetable.js:36-50` (fetchTimetable 교체)

- [ ] **Step 1: import 라인 제거**

`timetable.js` 1번째 줄을 삭제한다:

```
삭제 전:
import { NEIS_API_KEY, NEIS_ATPT_CODE, NEIS_SCHOOL_CODE } from './app.js';

삭제 후:
(해당 줄 없음 — 파일이 빈 줄 없이 ALL_SLOTS 정의로 시작)
```

- [ ] **Step 2: fetchTimetable 함수 교체**

`timetable.js`의 `fetchTimetable` 함수(36~51줄)를 아래로 교체한다:

```js
export async function fetchTimetable(classNum, date) {
  const jsDay = new Date(date + 'T00:00:00').getDay(); // 0=일, 6=토
  if (jsDay === 0 || jsDay === 6) return {};
  const comciganDay = jsDay - 1; // 0=월, 1=화, 2=수, 3=목, 4=금
  try {
    const res = await fetch(`/api/comcigan?class=${classNum}&day=${comciganDay}`);
    return await res.json();
  } catch {
    return {};
  }
}
```

- [ ] **Step 3: 커밋**

```bash
git add timetable.js
git commit -m "feat: fetchTimetable을 컴시간 API로 교체"
```

---

### Task 4: timetable.js — renderTimetable 표시 형식 수정

**Files:**
- Modify: `timetable.js:117` (subject 표시 로직)

현재 코드 (`timetable.js` 약 117번째 줄):
```js
const subject = fixed ? '' : (subjectMap[key] || '-');
```

- [ ] **Step 1: subject 표시 로직 수정**

위 줄을 아래로 교체한다:

```js
const entry = fixed ? null : subjectMap[key];
const subject = fixed ? '' : (entry
  ? (entry.teacher ? `${entry.subject}(${entry.teacher})` : entry.subject)
  : '-');
```

- [ ] **Step 2: 브라우저에서 시각적으로 확인**

로컬 서버(`npx vercel dev` 또는 `python3 -m http.server`)를 실행하고 시간표 페이지를 열어 교시별 과목이 `파이썬(김진)` 형식으로 표시되는지 확인한다.

- [ ] **Step 3: 커밋**

```bash
git add timetable.js
git commit -m "feat: 시간표 과목 표시를 '파이썬(김진)' 형식으로 수정"
```

---

### Task 5: NEIS 제거 — app.js, build.js

**Files:**
- Modify: `app.js:9-11`
- Modify: `build.js:5-7`

- [ ] **Step 1: app.js에서 NEIS export 3줄 제거**

`app.js`에서 아래 3줄을 삭제한다:

```js
// 삭제할 줄들:
export const NEIS_API_KEY = ENV.NEIS_API_KEY;
export const NEIS_ATPT_CODE = ENV.NEIS_ATPT_CODE;
export const NEIS_SCHOOL_CODE = ENV.NEIS_SCHOOL_CODE;
```

- [ ] **Step 2: build.js에서 NEIS env 주입 3줄 제거**

`build.js`에서 아래 3줄을 삭제한다:

```js
// 삭제할 줄들:
  NEIS_API_KEY: '${process.env.NEIS_API_KEY || ''}',
  NEIS_ATPT_CODE: '${process.env.NEIS_ATPT_CODE || ''}',
  NEIS_SCHOOL_CODE: '${process.env.NEIS_SCHOOL_CODE || ''}'
```

`build.js` 삭제 후 최종 모습:

```js
const fs = require('fs');
const content = `window.__ENV = {
  SUPABASE_URL: '${process.env.SUPABASE_URL || ''}',
  SUPABASE_ANON_KEY: '${process.env.SUPABASE_ANON_KEY || ''}'
};`;
fs.writeFileSync('env.js', content);
console.log('env.js generated');
```

- [ ] **Step 3: build.js 정상 실행 확인**

```bash
node build.js
```

Expected: `env.js generated` 출력, `env.js`에 NEIS 관련 키 없음.

- [ ] **Step 4: 커밋**

```bash
git add app.js build.js env.js
git commit -m "chore: NEIS API 관련 코드 및 환경변수 제거"
```

---

### Task 6: 최종 통합 확인

- [ ] **Step 1: vercel dev로 로컬 실행**

```bash
npx vercel dev
```

Expected: 로컬 서버 시작, `http://localhost:3000` 접근 가능.

- [ ] **Step 2: API 엔드포인트 직접 호출**

```bash
curl "http://localhost:3000/api/comcigan?class=1&day=1"
```

Expected: 1학년 1반 화요일 시간표 JSON.

- [ ] **Step 3: 브라우저에서 시간표 페이지 확인**

`http://localhost:3000/calendar.html` 접속 → 시간표 탭에서 1~5반 각각 클릭.
확인 항목:
- 과목이 `파이썬(김진)` 형식으로 표시됨
- 빈 교시는 `-`로 표시됨
- 주말은 슬롯 없음

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "feat: NEIS → 컴시간 알리미 시간표 연동 완료"
```
